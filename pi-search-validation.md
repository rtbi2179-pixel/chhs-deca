# PI Study Library Search Validation

The PI Study Library now exposes a labeled search field above the cluster and event-path results. Browser validation confirmed that entering `MKT-BL-050` reduces the Marketing library to the single matching performance indicator, and exposes an accessible clear-search control. Search results remain scoped to the active cluster or selected event study path.
